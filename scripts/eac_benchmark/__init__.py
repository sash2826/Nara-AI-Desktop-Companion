"""Expanded real-world file-organization benchmark generator.

Generates a standalone synthetic benchmark corpus under
``OneDrive - Volvo Group/Enterprise-AI-Companion-Benchmark`` plus a separate
downloads corpus and a set of floating (unorganised) files. Reuses the
declarative document-rendering helpers from ``scripts.benchmark_corpus.common``.

Run with::

    python -m scripts.eac_benchmark
"""
